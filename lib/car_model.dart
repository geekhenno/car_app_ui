import 'package:flutter/material.dart';

class CarModel {
  String title;
  String status;
  String time;
  Color dotColor;

  CarModel(
      {required this.title,
      required this.status,
      required this.time,
      required this.dotColor});
}
